# macOS
.DS_Store

# Claude Code local session data
.claude/

# Packaged extension build artifacts
*.crx
*.pem
*.zip

# Node
node_modules/
npm-debug.log*

# Cloudflare Wrangler local dev scratch (created by `wrangler dev` in site/)
.wrangler/

# Supabase CLI local state (created by `supabase link` / local dev — never commit)
supabase/.temp/
supabase/.branches/
supabase/.env
